# 特殊机制说明

## 伙伴重新在场时恢复资源

通过状态页将伙伴从“不在场”切换为“在场”时，系统会在同一次 MVU 数据写回中恢复该伙伴的三项资源：

- 生命值（HP）
- 法力值（MP）
- 体力值（SP）

恢复后的当前值为对应资源的总上限：`上限._基础 + 上限.额外`。

从在场切换为不在场不会改变资源值；已经在场时重复写入在场也不会触发额外处理。

此机制目前只覆盖状态页的在场快捷按钮。其他脚本或外部流程直接修改 MVU 变量时，不会自动触发恢复。
